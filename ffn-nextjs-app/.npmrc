# Suppress some deprecation warnings for indirect dependencies
# that we cannot directly control
audit-level=moderate
fund=false

# Use legacy peer deps to resolve some compatibility issues
legacy-peer-deps=false

# Prefer offline when possible
prefer-offline=true